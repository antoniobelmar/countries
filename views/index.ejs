<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Countries</title>
  </head>
  <body>
    Hello
    <table>
    <% for(var i=0; i < countryData.length; i++) { %>
       <tr>
         <td><%= countryData[i].name %></td>
       </tr>
    <% } %>
    </table>
  </body>
</html>
