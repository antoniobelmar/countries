<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Countries</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <table>
    <% for(var i=0; i < countryData.length; i++) { %>
       <tr>
         <td class="to-guess"><%= countryData[i].name %></td>
       </tr>
    <% } %>
    </table>
  </body>
</html>
